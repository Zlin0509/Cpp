#include "bits/stdc++.h"
using namespace std;
const int N = 105;
int a[N];
int n;
inline void Zlin()
{
    cin >> n;
    for (int i = 1; i <= n; i++)
        cin >> a[i];
    
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    Zlin();
    return 0;
}