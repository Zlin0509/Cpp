#include "bits/stdc++.h"
using namespace std;

using ll = long long;
using vi = vector<int>;
using vll = vector<ll>;

int n, m, l, r;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    cin >> n;

    cin >> m;
    while (m--)
    {
        cin >> l >> r;
        
    }

    return 0;
}